---
"astro-node-fastify": minor
---

Update dependencies: `fastify` to ^5.11.2 (adopting its `LogController` API for request-logging configuration), `@fastify/static` to v10, and `@fastify/compress` to ^9.2.0.
Widen the `astro` peer dependency range to `>= 7.0 <= 7.2`.
